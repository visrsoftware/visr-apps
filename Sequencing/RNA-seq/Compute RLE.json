{
  "label": "Relative Log Expression (RLE)",
  "info": "The log-ratio of the read count of each gene to the median read count across samples",
  "categories":[  {
    "label": "",
    "info": "",
    "variables": {
      "visr.param.columns": {
        "label": "Columns to calculate RLE for",
        "type": "multi-column-numerical"
      },
      "visr.param.addvalue": {
        "label": "Increment all counts by",
        "info": "The number to add to all counts (necessary if there is 0 in data)",
        "type": "double",
        "default": 0
      },
      "visr.param.scale": {
        "label": "Scale by library sizes",
        "info": "Calculate normalization factors and scale the raw library sizes.",
        "type": "boolean",
        "default": true
      },
      "visr.param.output_Normalized": {
        "label": "Size Normalized Output Columns Prefix",
        "info": "values of columns normalized by the size factor",
        "type": "output-multi-column",
        "default": "SizeNorm_"
      },
      "visr.param.output_RLE": {
        "label": "RLE Output Columns Prefix",
        "info": "RLE values of columns",
        "type": "output-multi-column",
        "default": "RLE_"
      }
    }
  }]
}
